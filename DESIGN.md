# SARAH SUNCARE 홈페이지 디자인 명세

## 1. 디자인 원칙

전체 인상은 **과학적, 깨끗함, 신뢰감, 프리미엄 선케어**다. 시각적 무게는 장식보다 명확한 타이포그래피, 넓은 여백, 하늘색 제품 패키지와 조화를 이루는 색면에서 만든다.

- 넓고 차분한 공간감을 유지한다.
- 네이비로 신뢰감을, 시안 블루로 청량함을, 연한 블루와 오프화이트로 깨끗함을 표현한다.
- 과한 그림자, 네온, 유리 효과의 남용, 복잡한 장식을 피한다.
- 참고 사이트의 레이아웃 감각을 복제하지 않고 SARAH SUNCARE만의 추상 비주얼 언어를 사용한다.

## 2. 디자인 토큰 제안

구현 시 접근성 대비를 다시 측정하고 필요하면 토큰 값을 소폭 조정한다.

### 색상

| 용도 | 제안 값 | 설명 |
| --- | --- | --- |
| Deep Navy | `#08243A` | 주요 본문, 제목, 강한 대비 |
| Cyan Blue | `#087DA4` | 브랜드 강조, 링크·포커스 보조; 흰색 대비 4.69:1 |
| Sky Blue | `#A9E3F5` | 제품 패키지 연계, 추상 도형 |
| Pale Blue | `#EAF8FC` | 부드러운 배경과 카드 변주 |
| Off White | `#F8FBFC` | 페이지 바탕 |
| White | `#FFFFFF` | 카드와 헤더 표면 |
| Muted Text | `#526774` | 보조 문구; 작은 텍스트 대비 확인 필수 |
| Border | `#D9E6EB` | 얇은 구분선 |

- 본문 텍스트는 밝은 배경에서 Deep Navy를 기본으로 한다.
- Cyan Blue는 넓은 면적의 작은 글자색으로 남용하지 않고 강조, 아이콘, 포커스 링 등에 사용한다.
- 포커스 링은 배경과 명확히 구분되는 2px 이상의 윤곽과 충분한 여백을 제공한다.

### 타이포그래피

- 한글 가독성이 검증된 시스템 sans-serif 또는 프로젝트에서 선택한 한글 웹폰트 한 계열을 사용한다.
- 시스템 폰트 우선 제안: `Pretendard`가 로컬에 있다고 가정하지 말고, 기본 스택은 `system-ui`, `-apple-system`, `BlinkMacSystemFont`, `"Segoe UI"`, `sans-serif`로 구성한다.
- 웹폰트를 도입할 경우 필요한 굵기만 로드하고 폰트 표시 지연을 최소화한다.
- 히어로 핵심 문구는 데스크톱에서 크고 단단하게, 모바일에서는 줄바꿈을 유지하면서 화면을 넘치지 않도록 `clamp()`로 조정한다.
- 본문 행간은 약 1.6~1.8, 제목 행간은 약 1.15~1.3을 기준으로 한다.
- 확정된 줄바꿈은 글자마다 `<br>`을 남용하지 않고 `CONTENT.md`의 의미 단위에만 적용한다.

### 간격과 폭

- 콘텐츠 최대 폭 제안: 약 1280~1440px.
- 좌우 여백은 모바일 20~24px, 태블릿 32~48px, 데스크톱 64px 이상을 유연하게 적용한다.
- 섹션 상하 여백은 모바일 약 80~112px, 데스크톱 약 128~180px 범위에서 조정한다.
- 8px 기반 간격 체계를 사용하되 타이포그래피의 시각 균형을 우선한다.

## 3. 공통 헤더

- 상단에 sticky를 우선 고려한다. `top: 0`과 적절한 `z-index`로 콘텐츠 위에 유지한다.
- 배경은 흰색 또는 약간 투명한 흰색이며, 투명도를 사용할 때도 텍스트 대비가 항상 유지되어야 한다.
- 하단에 1px 수준의 Border 색 구분선을 둔다. 진한 그림자는 사용하지 않는다.
- 높이 제안: 모바일 64~72px, 데스크톱 72~84px.
- 왼쪽 프로젝트명과 오른쪽 `Contact Us` 사이에 충분한 공간을 둔다. 중앙 메뉴는 만들지 않는다.
- 긴 프로젝트명은 작은 화면에서 글자 크기를 줄이거나 최대 폭을 제한하되 의미 있는 텍스트를 생략하지 않는다.
- 왼쪽 프로젝트명은 색상·서체·크기·배치를 유지한 홈 링크다. hover에서는 포인트 색상과 얇은 밑줄로 클릭 가능성을 알리고, 키보드 포커스에는 공통 포커스 링을 표시한다.
- 홈에서 프로젝트명을 선택하면 부드럽게 맨 위로 이동하며, `prefers-reduced-motion`에서는 스크롤 애니메이션을 제거한다.
- `Contact Us`는 이번 버전에서 테두리가 있는 네이티브 버튼으로 보여 주되 동작은 연결하지 않는다. 링크 밑줄이나 화면 이동을 암시하는 hover 이동 효과는 적용하지 않고, 준비 중 상태를 접근성 속성으로 전달한다.

## 4. 홈 히어로

### 레이아웃

- 헤더 아래 첫 주요 영역으로 배치하며 데스크톱에서 최소한 넉넉한 뷰포트 높이를 확보한다.
- 데스크톱: 텍스트 약 52~58%, 제품 이미지 약 42~48%의 2열 균형을 시작점으로 한다.
- 제품 이미지는 오른쪽에 크게 배치하되 컨테이너 안에서 `object-contain`을 사용한다. 원본 비율과 전체 형태가 항상 보이게 한다.
- 모바일: 텍스트를 먼저 읽고 제품 이미지가 이어지는 1열을 기본으로 한다. 시각 테스트에서 메시지 집중도가 더 좋을 경우 이미지를 핵심 문구 직후에 둘 수 있으나 DOM 읽기 순서가 자연스러워야 한다.
- 장식이 필요하면 흐린 원형, 얇은 궤도선, 연한 블루 색면처럼 제품과 경쟁하지 않는 CSS 추상 요소만 사용한다.

### 텍스트 계층

- 그룹 1: 첫 시선을 받는 굵은 핵심 제목.
- 그룹 2: 차분한 본문. 최대 행 길이를 제한해 한글 읽기 리듬을 유지한다.
- 그룹 3: 두 번째 굵은 강조 문구. 그룹 1보다 같거나 한 단계 작은 크기.
- 그룹 4: `SARAH SUNCARE`를 브랜드명으로 분명히 구분하고, 다음 줄을 브랜드 설명으로 배치한다.
- 정확한 문구와 줄바꿈은 `CONTENT.md`를 단일 기준으로 삼는다.

### 등장 모션

- 히어로가 관찰 영역에 들어오면 그룹 1→2→3→4 순서로 한 번 나타나는 방식을 제안한다.
- 초기 상태: `opacity: 0`, `transform: translateY(24px)`, 매우 약한 blur.
- 완료 상태: `opacity: 1`, `transform: translateY(0)`, blur 없음.
- 지속 시간 제안: 그룹당 약 500~700ms, stagger 약 100~160ms. 실제 체감이 느리면 더 짧게 조정한다.
- easing은 부드러운 ease-out 계열을 사용한다.
- 초기 서버 렌더링은 콘텐츠가 보이는 상태여야 한다. 모션 기능이 정상 준비된 경우에만 향상된 초기 상태를 적용해 JavaScript 실패 시 숨은 텍스트가 남지 않게 한다.
- `prefers-reduced-motion: reduce`에서는 transition, delay, blur, 이동, 이미지 부유 효과를 제거한다.
- 제품 부유 효과가 필요하면 작은 Y축 이동을 긴 주기로 한 번에 하나만 사용한다. 시차 효과는 스크롤 이벤트의 매 프레임 연산보다 CSS 또는 requestAnimationFrame으로 제한하며 필수 요소로 취급하지 않는다.

## 5. 프로젝트 살펴보기 섹션

- 히어로와 카드 사이에 충분한 오프화이트 또는 흰 여백을 둬 명확한 장면 전환을 만든다.
- `프로젝트 살펴보기` 제목과 간결한 도입 간격 뒤에 카드 그리드를 배치한다.
- 데스크톱은 동일한 너비의 3열, 모바일은 1열, 중간 폭에서는 가독성에 따라 1열 또는 2열 후 마지막 카드 확장을 선택할 수 있다. 단, 대형 데스크톱 최종 상태는 3열이어야 한다.

### 카드 구성

각 카드는 하나의 전체 영역 링크이며 다음 시각 순서를 가진다.

1. 두 자리 번호 `01`, `02`, `03`
2. 제목
3. 짧은 설명
4. 우측 또는 하단의 화살표 아이콘

- 카드 높이와 내부 패딩을 넉넉하게 유지한다.
- 카드마다 Pale Blue, White, 아주 옅은 Cyan 계열의 미묘한 변주를 줄 수 있다.
- 추상 도형은 CSS pseudo-element 또는 장식용 요소로 만들고 `aria-hidden="true"` 처리한다.
- 기본: 얇은 경계, 거의 없거나 매우 약한 그림자.
- hover: 경계색 변화, 화살표의 짧은 이동, 카드의 2~4px 이내 상승 중 일부를 절제해 사용한다.
- focus-visible: hover와 독립적으로 분명한 포커스 링을 제공한다.
- active: 이동량 또는 배경 명도를 살짝 줄여 눌린 상태를 표현한다.
- reduced motion에서는 위치 이동 없이 색·경계 변화만 즉시 제공한다.

## 6. 제품소개 페이지

### 공통 구성

- 홈의 `--navy`, `--cyan`, `--sky`, `--pale-blue`, `--off-white`, `--border` 토큰과 시스템 폰트, 최대 폭, 여백, 포커스 링을 그대로 재사용한다.
- 밝은 White/Off White 배경을 번갈아 쓰고 1px 경계와 여백으로 섹션을 구분한다. 산업환경 섹션만 어두운 별도 테마로 바꾸지 않는다.
- 모든 섹션은 두 자리 번호와 제목을 사용하며 제품 소개, 산업환경, 접근 과정, 기술, 평가, 성분 순으로 읽힌다.

### 제품 소개

- 데스크톱은 제품 이미지 45%, 정보 패널 55%의 2열이며 모바일은 제목, 부제, 제품, 정보 패널 순의 1열이다.
- 제품 뒤에는 Pale Blue 원형과 얇은 궤도선만 두고 이미지는 `object-contain`으로 표시한다.
- 정보는 하나의 흰 패널 안에서 `제품 표기`와 `프로젝트 검토 항목`으로 구분하며 얇은 선과 작은 선형 아이콘을 사용한다.

### 산업환경 카드

- 4열/2열/1열 그리드로 전환하며 모든 카드는 같은 구조와 시각 비중을 가진다.
- 기본 카드는 데스크톱에서 약 260~300px 너비와 410~470px 높이를 기준으로 하며, 위쪽 약 50~55%를 산업 장면 이미지가 차지한다.
- 이미지 아래에는 중앙 정렬한 짧은 한글 제목, 포인트 구분선, 두 줄 설명과 절제된 `상세 보기` 버튼을 둔다. 긴 영문 산업명·키워드 배지·장문 설명은 앞면에서 제외한다.
- 장면 이미지는 제공된 목표 카드 이미지에서 텍스트가 없는 상단 영역만 분리해 사용한다. 이미지에 원형 아이콘이 포함되어 있으므로 HTML 아이콘을 중복 배치하지 않는다.
- hover와 focus-within은 이미지의 미세 확대, 테두리와 그림자 강조만 제공한다. 상세 정보는 실제 버튼으로 열고 카드가 콘텐츠 높이만큼 자연스럽게 늘어나게 한다.

### 접근·기술·평가·성분

- 접근 과정은 네 단계를 위에서 아래로 연결하고 각 단계 사이에 얇은 선과 아래 방향 화살표를 둔다.
- 기술 원리는 하나의 넓고 밝은 패널 안에서 HIPE → Spike-emulsion → 카멜레온 구조로 연결한다. 각 단계의 번호·동일 크기 도형·제목·설명 기준선을 맞추고, 데스크톱은 가로 화살표 흐름, 모바일은 축소 없는 세로 흐름으로 전환한다.
- E-SKIN 원본 도식은 흰 패널에 비율을 유지해 배치하고, 모바일에서 원본 라벨이 축소되지 않도록 패널 내부 가로 탐색을 허용한다. 별도 텍스트 흐름은 항상 세로로 읽을 수 있다.
- 성분 섹션은 기존 색상과 타이포그래피를 유지하면서 투명 유리, 수분, 크림, 광학 질감의 고해상도 소재 렌더를 중심으로 구성한다. 기능군은 데스크톱 3열×2행, 태블릿 2열, 모바일 1열로 전환한다.
- 각 기능군은 큰 4:3 이미지, 영문 기능군명, 한글 요약 순서로 읽히며 두꺼운 박스보다 이미지의 부드러운 그림자와 넉넉한 여백으로 구분한다.
- hover와 focus-within에서는 이미지가 1.02~1.03배 확대되거나 4~6px 상승하고 그림자·밝기가 소폭 변한다. reduced motion에서는 이동과 등장 애니메이션을 제거한다.
- 기존 성분 정보는 기본 화면에 펼쳐 두지 않고, 여섯 비주얼 아래의 밝은 펼침 패널에서 확인한다. 네이티브 버튼, `aria-expanded`, 명확한 포커스 상태를 제공하며 패널은 콘텐츠 높이만큼 자연스럽게 늘어난다.

## 7. 프로젝트 소개 페이지

- 홈·제품소개와 같은 색상 토큰, 시스템 글꼴, 1280px 콘텐츠 폭, 섹션 여백과 얇은 경계를 재사용한다.
- 여덟 영역은 두 자리 번호, 영문 보조 제목, 큰 한글 제목, 핵심 설명 순의 일관된 제목 위계를 가진다.
- 프로젝트 개요는 본문과 단체사진을 교차 배치하고 네 수치는 데스크톱 4열, 모바일 2열 또는 1열로 전환한다.
- 네 수치의 기존 크기·색상·구분선은 유지한다. 숫자 영역에는 tabular 숫자와 목표 자릿수만큼의 최소 너비를 적용해 카운트업 중 단위와 주변 요소가 움직이지 않게 한다.
- 각 수치가 30% 이상 보일 때 숫자만 0에서 최종값으로 감속 증가하며 항목별 한 번만 실행한다. reduced motion에서는 최종값을 즉시 표시한다.
- 제품·기술 조사는 읽을 수 있는 크기의 기업 제공 개념도와 세 설명 묶음을 함께 배치한다. 개념도를 장식 배경처럼 자르지 않는다.
- 산업환경은 모든 내용을 작은 카드로 분절하지 않고 2열의 정돈된 비교 목록과 강조 결과 영역으로 구성한다.
- 특허 섹션은 검색 결과 → 중복·법적 상태 검토 → 최종 288건의 선별 흐름과 국가·권역별 가로 막대그래프를 사용한다.
- 실험은 날짜순 기록, 실제 사진, 비교군 3개, 과정·조건을 교차 배치한다. 사진은 인물과 시료가 잘리지 않도록 비율을 유지한다.
- 협업 기록은 세로 타임라인으로 구성하고 날짜·방식, 본문, 후속 방향, 관련 사진을 같은 읽기 흐름 안에 둔다. 저해상도 교육 화면은 원본 크기 이상으로 확대하지 않는다.
- 콘텐츠 결과물은 실제 링크가 확인되지 않은 상태를 시각적으로 명시하며, 참여 학생과 멘토는 별도 영역으로 구분한다.

## 8. 시장분석 페이지

- 홈·제품소개·프로젝트 소개와 같은 토큰, 시스템 글꼴, 1280px 콘텐츠 폭과 섹션 여백을 재사용한다.
- 여섯 영역은 큰 제목과 숫자, 실제 조사 이미지, 관계 도식과 비교 행을 번갈아 사용해 보고서형 단일 표의 인상을 피한다.
- 첫 영역의 세 조사 범위는 중앙 연구 관점과 연결된 개념도로 표현하되 면적이 시장 규모로 오해되지 않게 캡션을 둔다.
- 시장 신호는 `+281%`를 시각 중심에 두고 기간·비교 기준·공식 출처를 같은 화면에 고정한다.
- 공급 사례는 사진과 설명을 교차 배치하며 기사 캡처는 네이티브 `details` 보조 영역에서만 제공한다.
- 사용자·구매자는 데스크톱 2열, 모바일 1열로 전환하고 핵심 조건은 처음부터 노출하며 추가 검증만 펼침 영역에 둔다.
- 검증 과제는 현재 참고 방식 → 추가 확인 과제를 가로 비교하고 모바일에서는 위아래 흐름으로 전환한다.
- 마지막 제안은 옥외작업 우선 검토를 네이비 강조 패널로, 세 추가 검증 분야를 밝은 비교 목록으로 구분한다.
- hover에 의존하는 정보를 만들지 않고 모든 링크·`summary`에 전역 focus-visible 링을 제공한다.

## 9. 반응형 동작 기준

구체적인 breakpoint는 구현 환경의 Tailwind 기본값을 시작점으로 삼고 콘텐츠가 깨지는 지점을 기준으로 조정한다.

| 범위 | 핵심 동작 |
| --- | --- |
| 작은 모바일 | 단일 열, 최소 20px 좌우 여백, 헤더 텍스트 충돌 방지, 카드 전체 폭 |
| 큰 모바일/태블릿 | 유연한 단일 열 또는 카드 2열 검토, 과도한 줄 길이 제한 |
| 데스크톱 | 히어로 2열, 탐색 카드 3열, 넓은 섹션 여백 |
| 대형 데스크톱 | 콘텐츠 최대 폭 유지, 빈 공간을 확대하되 텍스트 행 길이는 유지 |

최소 수동 확인 폭 제안: 320px, 375px, 768px, 1024px, 1440px. 화면 회전, 200% 확대, 긴 한글 텍스트에서도 가로 스크롤과 겹침이 없어야 한다.

## 10. 접근성 시각 체크

- 일반 텍스트 4.5:1, 큰 텍스트 3:1 이상의 명도 대비를 목표로 한다.
- 키보드 포커스가 sticky 헤더 뒤에 가려지지 않도록 `scroll-margin-top` 등을 고려한다.
- 터치 대상은 가능한 한 최소 44×44px을 확보한다.
- 화살표 아이콘만으로 링크 이름을 만들지 않고 카드 제목이 접근 가능한 이름에 포함되게 한다.
- 줌과 텍스트 확대 시 문구가 잘리거나 카드 높이가 고정되어 넘치지 않게 한다.
